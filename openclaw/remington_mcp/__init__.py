"""Remington's MCP tool layer: `trinity_mcp` (Atlassian/Bitbucket) + `pm_mcp` (PM
routines over the pure `pm_core`). Launched as MCP servers by the OpenClaw gateway.
Package name is `remington_mcp` (not `mcp`) to avoid shadowing the MCP SDK.
"""
