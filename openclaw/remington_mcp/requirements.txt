# Remington OpenClaw MCP tool layer
# Installed into the gateway's OpenClaw python env alongside the `trinity` package.

mcp>=1.2.0            # MCP Python SDK (FastMCP server)
requests>=2.31.0
pytz>=2024.1

# trinity is installed separately (editable) from ~/code/citemed/atlassian-trinity.
# It provides the Atlassian/Bitbucket primitives that trinity_mcp wraps and that
# pm_core calls. Do NOT vendor it here — import from `trinity.*`.
